Today I learned that in Pandas, many functions like `fillna()` and `get_dummies()` don’t change the DataFrame unless I assign them back, which was my main mistake. I also understood binning using `pd.cut()` to convert numbers into categories, and encoding (especially one-hot encoding) to convert text into numbers for ML. I realized I should clean data before applying transformations and think carefully about each step instead of just writing code and hoping it works.
